<div class="form-group">
    <%= f.label :strain_id %>
    <!-- Opcion 1 - Esta opcion hace un mapeo desde la vista -->
    <%#= f.select :strain_id, options_from_collection_for_select(@strains, :id, :name) %>
    <!-- Opcion 2 -->
    <%#= f.select :strain_id, @strains_two %>
    <!-- Opcion 3 - Recoemndado para que se entienda lo que se esta haciendo-->
    <%#= f.select :strain_id, options_for_select(@strains_two) %>
    <!-- Opcion 4 - Con esto el edit mantiene los valores originales -->
    <%= f.collection_select :strain_id, @strains_three, :id, :name %>
</div>

<div class="form-group">
    <%= f.label :percentage %>
    <%= f.number_field :percentage %>
</div>